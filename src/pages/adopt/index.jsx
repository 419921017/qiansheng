import Taro from "@tarojs/taro";
import { View } from "@tarojs/components";

const Adopt = props => {
  return <View>Search</View>;
};

Adopt.config = {
  navigationBarTitleText: "领养活动"
};

export default Taro.memo(Adopt);
